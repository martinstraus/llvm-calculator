enum Token {
    EOF = 0,
    FUNC = 1,
    IDENTIFIER = 2
};
/*
struct FilePosition {
    int line, column;
};

struct TokenValue {
    Token token;
    FilePosition position;
    std::string stringValue;
};

struct LexerStatus {
    FilePosition filePosition;
    Token currentToken;
};*/